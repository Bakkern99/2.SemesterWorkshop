@page "/"
@using Shared

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

@code
{
    
    private Tøj[]? tøj;

    protected override async Task OnInitializedAsync()
    {
        await LoadAll();
    }

    private async Task LoadAll()
    {
        tøj = await mService.GetAll();
    }

    private void NavigateToAdd()
    {
        navMan.NavigateTo("additem");
    }

    private async Task Delete(Tøj tøj)
    {
        await mService.Delete(tøj.Id);
        await LoadAll();

    }

}
